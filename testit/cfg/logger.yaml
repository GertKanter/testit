###############################################
#                                             #
#       Configuration template (schema)       #
#                                             #
#  PLEASE LOOK AT THE TUTORIALS FOR EXAMPLES  #
#                                             #
###############################################

configuration:
  triggers: # Trigger log entry
    - identifier: "" # topic/service name (with full namespace, i.e., "/robot/odom")
      type: "" # topic/service as string (e.g., "std_msgs/Int32")
      mode: "" # "topic"/"service"
  inputs: # SUT inputs
    - identifier: "" # topic/service name (with full namespace, i.e., "/robot/odom")
      type: "" # topic/service as string (e.g., "std_msgs/Int32")
      mode: "" # "topic"/"service"
  outputs: # SUT outputs (monitored values)
    - identifier: "" # topic/service name (with full namespace, i.e., "/robot/odom")
      type: "" # topic/service as string (e.g., "std_msgs/Int32")
      mode: "" # "topic"/"service"
